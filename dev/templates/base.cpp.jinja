/*
 Do not edit, this is autogenerated
 {{ license_notice }}
*/
#pragma once

#include <cstdint>
#include <string>

static
std::string id2str(uint8_t id) {
  {% for mid, name in ids.items() -%}
  if (id == {{ mid }}) return "{{ name|upper }}";
  {% endfor -%}
  return "UNKNOWN";
}